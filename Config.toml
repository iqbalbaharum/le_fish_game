modules_dir = "artifacts/"

[[module]]
    name = "sqlite3"

    [module.wasi]
    preopened_files = ["/tmp"]
    mapped_dirs = { "tmp" = "/tmp" }
    
[[module]]
    name = "lefish_sqlite"
    mem_pages_count = 100
    logger_enabled = true

[[module]]
    name = "lefish_game"
    mem_pages_count = 100
    logger_enabled = true

    [module.mounted_binaries]
    curl = "/usr/bin/curl"

