module LefishGame declares *

data Mint:
  id: string
  to: string
  block_no: string

service LefishGame("lefish"):
  battle()  
  decay()  
  feed()  
  get_latest_mint(uri: string) -> []Mint
  mint() -> string
